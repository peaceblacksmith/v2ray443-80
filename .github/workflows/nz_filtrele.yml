name: New Zealand V2ray Filter

on:
  schedule:
    - cron: '30 * * * *'
  workflow_dispatch:
  push:
    branches: [ main, master ]

jobs:
  filter-nz:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install geoip2==4.8.0 maxminddb==2.5.2

      - name: Create Filter Script
        run: |
          cat > nz_filter.py << 'PYEOF'
          import base64
          import json
          import re
          import sys
          import socket
          import ssl
          import datetime
          import urllib.request
          import os
          import traceback
          from concurrent.futures import ThreadPoolExecutor, as_completed
          import geoip2.database

          DB_URL = "https://raw.githubusercontent.com/Loyalsoldier/geoip/release/GeoLite2-Country.mmdb"
          DB_PATH = "GeoLite2-Country.mmdb"
          URLS = [
              "https://raw.githubusercontent.com/barry-far/V2ray-config/main/All_Configs_Sub.txt",
              "https://raw.githubusercontent.com/MatinGhanbari/v2ray-configs/main/subscriptions/v2ray/all_sub.txt",
          ]

          ssl_ctx = ssl.create_default_context()
          ssl_ctx.check_hostname = False
          ssl_ctx.verify_mode = ssl.CERT_NONE

          def download_db():
              print("📥 GeoIP Veritabanı indiriliyor...")
              try:
                  req = urllib.request.Request(DB_URL, headers={'User-Agent': 'Mozilla/5.0'})
                  with urllib.request.urlopen(req, timeout=120, context=ssl_ctx) as response:
                      with open(DB_PATH, 'wb') as f:
                          f.write(response.read())
                  size = os.path.getsize(DB_PATH)
                  print(f"✅ İndirildi. Boyut: {size / 1024 / 1024:.2f} MB")
                  if size < 2000000:
                      raise Exception(f"Dosya çok küçük ({size} byte). Bozuk indirme.")
              except Exception as e:
                  print(f"❌ DB İNDİRME HATASI: {e}")
                  sys.exit(1)

              print("🔍 Veritabanı doğrulanıyor (8.8.8.8 -> US)...")
              try:
                  reader = geoip2.database.Reader(DB_PATH)
                  resp = reader.country('8.8.8.8')
                  if resp.country.iso_code != 'US':
                      raise Exception(f"Yanlış DB türü. Beklenen: US, Alınan: {resp.country.iso_code}")
                  reader.close()
                  print("✅ Veritabanı geçerli.")
              except Exception as e:
                  print(f"❌ DB DOĞRULAMA HATASI: {e}")
                  sys.exit(1)

          def fetch_url(url):
              try:
                  req = urllib.request.Request(url, headers={'User-Agent': 'Mozilla/5.0'})
                  with urllib.request.urlopen(req, timeout=60, context=ssl_ctx) as response:
                      return response.read().decode('utf-8', errors='replace')
              except Exception as e:
                  print(f"⚠️  {url} indirilemedi: {e}")
                  return ""

          def resolve_to_ip(host):
              host = host.strip()
              if not host:
                  return None
              try:
                  socket.inet_aton(host)
                  return host
              except socket.error:
                  pass
              try:
                  socket.inet_pton(socket.AF_INET6, host)
                  return host
              except socket.error:
                  pass
              try:
                  results = socket.getaddrinfo(host, None, socket.AF_UNSPEC, socket.SOCK_STREAM)
                  for family, _, _, _, sockaddr in results:
                      ip = sockaddr[0]
                      if ip.startswith(('127.', '10.', '192.168.', '172.', '0.')) or ip == '::1' or ip.startswith('fe80:'):
                          continue
                      return ip
              except Exception:
                  pass
              return None

          def extract_host_vmess(line):
              try:
                  b64 = line[8:].strip()
                  b64 += '=' * (-len(b64) % 4)
                  data = json.loads(base64.b64decode(b64).decode('utf-8', errors='replace'))
                  return data.get('add', '').strip() or None
              except Exception:
                  return None

          def extract_host_other(line):
              m = re.match(r'^[a-z0-9]+://(?:[^@/]+@)?(\[[^\]]+\]|[^:?/#]+)', line.strip(), re.IGNORECASE)
              if m:
                  h = m.group(1).strip()
                  if h.startswith('[') and h.endswith(']'):
                      h = h[1:-1]
                  return h or None
              return None

          def extract_host(line):
              line = line.strip()
              if not line or '://' not in line:
                  return None
              low = line.lower()
              if low.startswith('vmess://'):
                  return extract_host_vmess(line)
              if any(low.startswith(p) for p in ['vless://', 'trojan://', 'ss://', 'hysteria2://', 'hy2://', 'tuic://']):
                  return extract_host_other(line)
              return None

          def check_nz(cfg, reader, debug):
              host = extract_host(cfg)
              if not host:
                  return None
              ip = resolve_to_ip(host)
              if not ip:
                  return None
              try:
                  resp = reader.country(ip)
                  if resp.country.iso_code == 'NZ':
                      if len(debug) < 5:
                          debug.append(f"✅ NZ: {host} -> {ip}")
                      return cfg
              except Exception:
                  pass
              return None

          def main():
              download_db()

              print("\n🔄 Abonelikler indiriliyor...")
              contents = []
              with ThreadPoolExecutor(max_workers=4) as ex:
                  futs = {ex.submit(fetch_url, u): u for u in URLS}
                  for f in as_completed(futs):
                      r = f.result()
                      if r:
                          contents.append(r)
              print(f"✅ Kaynaklar: {len(contents)}/{len(URLS)}")

              if not contents:
                  with open('nz_sub.txt', 'w') as f:
                      f.write('')
                  return

              reader = geoip2.database.Reader(DB_PATH)
              protos = ['vmess://', 'vless://', 'trojan://', 'ss://', 'hysteria2://', 'hy2://', 'tuic://']
              configs = []
              for c in contents:
                  for line in c.splitlines():
                      line = line.strip()
                      if line and '://' in line and any(line.lower().startswith(p) for p in protos):
                          configs.append(line)
              print(f"📊 Taranacak: {len(configs)} config")

              nz_list = []
              debug = []
              done = 0
              with ThreadPoolExecutor(max_workers=50) as ex:
                  futs = {ex.submit(check_nz, c, reader, debug): c for c in configs}
                  for f in as_completed(futs):
                      r = f.result()
                      if r:
                          nz_list.append(r)
                      done += 1
                      if done % 2000 == 0:
                          print(f"⏳ {done}/{len(configs)} | NZ: {len(nz_list)}")

              print(f"\n🎯 Toplam NZ: {len(nz_list)}")
              if debug:
                  print("\n🔍 DEBUG (ilk 5):")
                  for d in debug:
                      print(d)
              else:
                  print("\n⚠️ Hiç NZ sunucu bulunamadı.")

              seen = set()
              unique = [c for c in nz_list if not (c in seen or seen.add(c))]
              print(f"✅ Tekil NZ: {len(unique)}")

              ts = datetime.datetime.utcnow().strftime('%Y-%m-%d %H:%M:%S UTC')
              body = '\n'.join(unique) + f'\n# Last updated: {ts}\n' if unique else f'# No NZ servers at {ts}\n'
              with open('nz_sub.txt', 'w') as f:
                  f.write(base64.b64encode(body.encode('utf-8')).decode('utf-8'))
              print("💾 nz_sub.txt oluşturuldu.")
              reader.close()

          if __name__ == '__main__':
              try:
                  main()
              except Exception as e:
                  print(f"\n❌ FATAL: {e}")
                  traceback.print_exc()
                  sys.exit(1)
          PYEOF

      - name: Execute Filter Script
        run: python3 nz_filter.py

      - name: Commit and Push Changes
        run: |
          git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add nz_sub.txt
          if git diff --cached --quiet; then
            echo "⚠️ Değişiklik yok, commit atlanıyor."
          else
            git commit -m "🇳🇿 NZ update [$(date +'%Y-%m-%d %H:%M UTC')]"
            git push origin HEAD
            echo "✅ nz_sub.txt commit edildi."
          fi

